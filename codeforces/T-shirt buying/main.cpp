#include <algorithm>
#include <map>
#include <pair>

int main()
{
	int n;
	map <int, int> shirts;

	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> p >> a >> b;
		map.insert(pair<int, int>(p, a * 10 + b));
	}
	for (int i = 0; i < n; i++)
	{

	}
}
